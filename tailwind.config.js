module.exports = {
  purge: ['./src/**/*.html', './src/**/*.jsx'], // Specify the files to purge (remove unused CSS classes)
  darkMode: false, // or 'media' or 'class'
  theme: {
    extend: {},
  },
  variants: {
    extend: {},
  },
  plugins: [],
};
